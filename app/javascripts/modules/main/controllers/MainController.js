(function (window) {
    define([], function () {
        var MainController = function ($scope, $location) {

        };

        MainController.$injection = ['$scope', '$location'];

        return MainController;
    });
}(this));
