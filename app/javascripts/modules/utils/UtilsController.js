(function (window) {
    define([], function () {
        var UtilsController = function ($scope, $location) {

        };

        UtilsController.$injection = ['$scope', '$location'];

        return UtilsController;
    });
}(this));
